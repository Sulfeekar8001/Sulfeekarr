name: my-app
on:
  push:
    branches:
      - main
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: 

      - name: run simple command
        run: ls -a
      
